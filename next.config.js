/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // IMPORTANT: Replace 'Ankitha-reddy---portfolio' with your exact repository name.
  basePath: '/Ankitha-reddy---portfolio',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
